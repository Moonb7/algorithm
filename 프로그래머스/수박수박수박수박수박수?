public class Solution {
    public string solution(int n)
    {
        string answer = "";
        char[] chars = { '수', '박' };
        for (int i = 0; i < n; i++)
        {
            answer += i % 2 == 0 ? '수' : '박';
        }    
        return answer;
    }
}
