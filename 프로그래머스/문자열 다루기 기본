public class Solution {
    public bool solution(string s) {
        bool answer = false;
        if (4 == s.Length || 6 == s.Length) 
        {
            answer = int.TryParse(s, out int result);
        }
        return answer;
    }
}

TryParse를 이용해서 s문자열이 숫자로만 이루어지면 제대로 형변환이 되면서 되면 true 안되면 false로 result 변수에 반환하여 줍니다.


